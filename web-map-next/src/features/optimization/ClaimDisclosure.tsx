/**
 * The published claim, the Minimum Cost disclosure and the non-claims for a run (WP6A-1, owned by L1).
 *
 * <p>Stage 0 reserves the component and its place in the results sheet so L1 can write the copy
 * without touching any file another level owns. Until the G0 claim is frozen it renders nothing.
 */
export function ClaimDisclosure() {
  return null;
}
