# Journal — 2026-08-13
**Author:** ARK

## What I Built Today
- Delivered **PY-020** through a new `POST /api/v2/optimise` FastAPI endpoint. The endpoint maps the MVP project payload into the optimisation workflow and returns a typed, domain-oriented result.
- Kept `POST /api/v1/optimise` available by adapting its legacy request and response contract to the same workflow, so existing MVP integrations remain compatible.
- Added request/response mapping schemas for the v2 contract, including structured input-validation and internal-error responses with request IDs.
- Registered the v2 router and configurable `/api/v2` prefix in the application.
- Updated the optimiser so only the selected winning candidate is packaged for presentation, avoiding unnecessary work for non-winning candidates.
- Added a pre-allocation cell limit to the GIS cost-surface builder to reject oversized rasters before NumPy allocates memory.

## Tests Added or Updated
- Added an MVP v2 fixture and endpoint coverage for success, invalid input, partial success, no feasible candidate, workflow failure, determinism, and continued v1 OpenAPI support.
- Updated legacy API and orchestration tests for the compatible mapping and winner-only presentation packaging.
- Added a regression test confirming the cost-surface maximum-cell check runs before allocation.

## Documentation Updated
- Updated the MVP, Python-engine architecture, roadmap, ticket-plan, presentation-boundary, dashboard, API, and Python-engine Obsidian notes to describe the PY-020 API/workflow delivery.

## Decisions Made
- Preserve the established v1 API contract while exposing the richer workflow through v2.
- Bound raster construction before memory allocation to prevent excessive cost-surface sizes from exhausting resources.

## Verification Notes
- The committed change (`1f9b00d`, `feat(optimisation): expose PY-020 workflow through compatible MVP API`) is clean in the working tree.
- Git history does not record the test commands or their outcomes, so this journal does not claim a test run was completed today.

## Next Steps
- Run the focused optimiser test suite and verify the v1/v2 endpoints against the MVP fixture in the target environment.
