"""Evidence tooling: canonical fingerprints and the controlled-cohort runner.

Shapes are defined in ``app.contracts.evidence``. Each module here names the
level that implements it; Stage 0 leaves them as unimplemented stubs.
"""
