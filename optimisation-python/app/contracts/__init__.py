"""Stage 0 contract pack for the SURGE demo optimisation work.

Everything in this package is frozen once ``demo-opt-base`` is tagged. It defines
names, shapes and codes that more than one worktree level implements against;
implementations live in level-owned modules. Change it only through a contract
change request recorded in ``contracts/CHANGELOG.md`` at the repository root.
"""

CONTRACT_PACK_VERSION = "1.0.0"
