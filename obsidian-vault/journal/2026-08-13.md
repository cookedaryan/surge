# Journal — 2026-08-13
**Author:** ARK

## What I Built Today
- Delivered **PY-020** through a typed `POST /api/v2/optimise` FastAPI endpoint, while preserving `POST /api/v1/optimise` through a compatible adapter. The workflow now packages only the selected winner and rejects oversized GIS cost surfaces before NumPy allocation.
- Completed **PY-021–PY-023**: constraint-aware routing support, fixture provenance labelling, and presentation/result-model updates required for the MVP optimisation flow.
- Implemented **PY-024/PY-025** GeoJSON pole presentation and endpoint deduplication. Shared topology endpoints become deterministic physical `junction` structures while route-local poles and spans retain their original traceability.
- Integrated canonical pole-network generation into the optimisation orchestrator for the recommended network, with structured failure handling when placement cannot be generated.
- Continued the in-progress pole work with strict pairwise tolerance clustering, stable junction IDs, source feeder/segment provenance, and physical-pole counts separated from route span counts.

## Tests Added or Updated
- Added coverage for v2 API outcomes, v1 compatibility, constraint regressions, fixture provenance, GeoJSON pole presentation, and winner-only packaging.
- Added pole-deduplication tests for shared substations/WTGs, same-feeder branches, tolerance boundaries, non-merging cases, deterministic ordering, and prevention of transitive over-merging.
- Added orchestration tests that verify pole generation uses the winning PNC geometry, preserves electrical/scoring results, is deterministic, and returns a structured failure on placement errors.

## Documentation Updated
- Updated the MVP, ticket plan, routing, pole-placement, presentation-boundary, and Python-engine notes in both the repository docs and Obsidian vault to reflect the delivered and in-progress work.

## Decisions Made
- Keep v1 stable while exposing the richer workflow through v2.
- Treat topology identity plus projected-coordinate tolerance as the condition for endpoint merging; do not merge nearby but unrelated or mid-route poles.
- Generate poles only after a winner is selected so placement cannot affect scenario generation, electrical analysis, or scoring.

## Verification Notes
- ARK commits recorded today: `1f9b00d` (PY-020), `854e46e` (PY-021–PY-023), `b8e3362` (documentation), and `067ad8b` (PY-024/PY-025).
- A further uncommitted implementation and documentation update is present in the working tree and is included above. No test command or result is evidenced by the available Git history, so this entry does not claim completed test execution.

## Next Steps
- Run the focused optimisation, pole-placement, presentation, and API tests; then review and commit the in-progress pole-network integration and documentation update.
