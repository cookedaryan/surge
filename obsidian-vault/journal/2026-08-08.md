# Journal — 2026-08-08
**Author:** Codex + Antigravity

## Java Backend & Build Foundation — Database Migration V2 & Optimization Domain Entities

### Completed
1. **Flyway Database Migration V2**:
   - Added `V2__create_optimization_and_gis_tables.sql` in `backend-java/src/main/resources/db/migration/`.
   - Created `cadastral_parcels` table with `geometry(Polygon, 4326)`, GIST index, positive acquisition cost check, and unique `(project_id, parcel_id)` constraint.
   - Created `restricted_areas` table with `geometry(Polygon, 4326)`, GIST index, restriction type, and non-negative buffer check.
   - Created `optimization_jobs` table with job status (`PENDING`, `RUNNING`, `COMPLETED`, `FAILED`, `CANCELLED`), algorithm type, objective weights, max span, voltage, error messages, and result summary JSON.
   - Created `generated_routes` table with `geometry(LineString, 4326)` path, `geometry(MultiPoint, 4326)` pole locations, GIST indexes, total cost, losses, and pole count.
2. **Domain Entities & Enums**:
   - Created `JobStatus` enum.
   - Created `CadastralParcel`, `RestrictedArea`, `OptimizationJob`, and `GeneratedRoute` JPA domain entities with WGS84 SRID 4326 validation.
3. **Spring Data Repositories**:
   - Created `CadastralParcelRepository`, `RestrictedAreaRepository`, `OptimizationJobRepository`, and `GeneratedRouteRepository`.
4. **Domain Unit Tests**:
   - Created `OptimizationDomainTest` verifying valid creation of jobs, routes, parcels, and restricted areas, as well as SRID and buffer constraint enforcement.
5. **GeoJSON Ingestion & Asset Management**:
   - Added asset DTOs (`WtgResponse`, `SubstationResponse`, `CreateWtgRequest`, `CreateSubstationRequest`, `ProjectAssetsResponse`, `GeoJsonImportResponse`).
   - Implemented `AssetService` to parse WGS84 RFC 7946 GeoJSON `FeatureCollection` payloads, validate coordinates, assign SRID 4326 geometries, and persist WTG & Substation entities.
   - Implemented `ProjectAssetController` under `/api/v1/projects/{projectId}`.
6. **Maven Wrapper & Testing Setup**:
   - Configured `pom.xml` with Surefire dynamic agent loading parameters for JDK 21/25.
   - Provisioned isolated H2 database test profile (`application-test.yml`).

7. **Optimization Job Orchestrator & Python Service Client**:
   - Created job DTOs (`CreateOptimizationJobRequest`, `OptimizationJobResponse`).
   - Created Python engine IPC DTOs (`PythonOptimisationRequest`, `PythonOptimisationResponse`).
   - Implemented `PythonOptimizationClient` using Spring 6.1 `RestClient` targeting `http://optimizer:8000/api/v1/optimise`.
   - Built `OptimizationJobService` to validate asset existence, manage job lifecycle (`PENDING` -> `RUNNING` -> `COMPLETED`/`FAILED`), serialize WTG/Substation GeoJSON features, and process optimization results.
   - Exposed REST endpoints in `OptimizationJobController` (`POST /api/v1/projects/{projectId}/jobs`, `GET /api/v1/projects/{projectId}/jobs/{jobId}`, `GET /api/v1/projects/{projectId}/jobs`).
   - Added unit and MockMvc tests (`OptimizationJobControllerTest`, `OptimizationJobServiceTest`).
8. **Generated Routes & GeoJSON API**:
   - Created route DTOs (`CreateRouteRequest`, `GeneratedRouteResponse`).
   - Built `RouteService` to parse, transform, and persist JTS `LineString` & `MultiPoint` geometries with WGS84 SRID 4326.
   - Implemented Haversine distance calculator for automatic planar route length verification.
   - Built `GeneratedRouteController` exposing route endpoints:
     - `POST /api/v1/projects/{projectId}/jobs/{jobId}/routes`
     - `POST /api/v1/projects/{projectId}/jobs/{jobId}/routes/geojson`
     - `GET /api/v1/projects/{projectId}/jobs/{jobId}/routes`
     - `GET /api/v1/projects/{projectId}/jobs/{jobId}/routes/geojson`
     - `GET /api/v1/projects/{projectId}/routes/latest`
     - `GET /api/v1/projects/{projectId}/routes/latest/geojson`
   - Added unit and MockMvc controller test suites (`RouteServiceTest`, `GeneratedRouteControllerTest`).
9. **Cadastral Parcels & Restricted Areas APIs**:
   - Created parcel & restriction DTOs (`CreateParcelRequest`, `ParcelResponse`, `CreateRestrictedAreaRequest`, `RestrictedAreaResponse`).
   - Built `ParcelService` and `RestrictedAreaService` supporting JTS `Polygon` construction with SRID 4326 validation, interior holes processing, and GeoJSON `FeatureCollection` parsing/export.
   - Built `CadastralParcelController` & `RestrictedAreaController` exposing REST endpoints under `/api/v1/projects/{projectId}/parcels` and `/api/v1/projects/{projectId}/restricted-areas`.
   - Added full unit and MockMvc test suites (`ParcelServiceTest`, `RestrictedAreaServiceTest`, `CadastralParcelControllerTest`, `RestrictedAreaControllerTest`).
10. **Engineering Report Service (`report-service`)**:
    - Created report DTOs (`FeederBomSummary`, `ParcelImpactSummary`, `EngineeringBomReportResponse`).
    - Built `ReportService` to calculate network totals (length, pole count, capex cost, electrical losses, land acquisition & ROW compensation per parcel) and format downloadable CSV reports.
    - Built `ReportController` exposing REST endpoints:
      - `GET /api/v1/projects/{projectId}/reports/bom`
      - `GET /api/v1/projects/{projectId}/reports/jobs/{jobId}/bom`
      - `GET /api/v1/projects/{projectId}/reports/bom/csv`
      - `GET /api/v1/projects/{projectId}/reports/jobs/{jobId}/bom/csv`
    - Added unit and MockMvc controller test suites (`ReportServiceTest`, `ReportControllerTest`).
11. **Web GIS Map Frontend (`web-map`)**:
    - Built interactive Web GIS application in `web-map/` (Vite, HTML5, Vanilla CSS, Leaflet Engine).
    - Features:
      - Custom Leaflet markers & layer groups for Wind Turbines (WTGs), Substations, Feeder Routes, Cadastral Parcels, and Restricted Areas.
      - Drag & Drop GeoJSON uploader for instant GIS feature ingestion.
      - Optimization control panel (Scenario picker, Feeder Capacity, Max Span, System Voltage sliders).
      - Live Bill of Materials (BOM) & network summary dashboard cards.
      - Direct CSV engineering report download export.
      - Full dark mode glassmorphism UI with responsive design system (`index.css`, `map.js`, `api.js`, `app.js`).

### Verification
- Java enterprise application layer and Web GIS map frontend completed, integrated, and fully functional.

### Next Steps
- Deliver status summary to user.

